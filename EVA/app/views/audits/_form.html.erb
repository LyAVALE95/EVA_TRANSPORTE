<%= form_with(model: audit, local: true) do |form| %>
  <% if audit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(audit.errors.count, "error") %> prohibited this audit from being saved:</h2>

      <ul>
      <% audit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="col-md-12 block formulary">
        <div class="col-md-12 block">
          <div class="form-group">
            <%= form.label :results,"Resultados", class: "control-label  col-sm-1" %>
              <div class="col-sm-11 ">
                <%= form.text_field :results, id: :audit_results, class: "form-control border" %>
              </div>
          </div>
          <div class="form-group">
            <%= form.label :description ,"Descripción", class: "control-label col-sm-1" %>
              <div class="col-sm-11">
                <%= form.text_field :description, id: :audit_description, class: "form-control border" %>
              </div>
          </div>
         
     
           
        </div>

      </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
