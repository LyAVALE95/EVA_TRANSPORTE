<%= form_with(model: carrier_line, local: true) do |form| %>
  <% if carrier_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carrier_line.errors.count, "error") %> prohibited this carrier_line from being saved:</h2>

      <ul>
      <% carrier_line.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
