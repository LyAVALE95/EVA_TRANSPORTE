<%= form_with(model: rate_expense, local: true) do |form| %>
  <% if rate_expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rate_expense.errors.count, "error") %> prohibited this rate_expense from being saved:</h2>

      <ul>
      <% rate_expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="col-md-12 block formulary">
        <div class="col-md-12 block">
          <div class="form-group">
            <%= form.label :name,"Nombre", class: "control-label  col-sm-1" %>
              <div class="col-sm-11 ">
                <%= form.text_field :name, id: :audit_results, class: "form-control border" %>
              </div>
          </div>
          <div class="form-group">
            <%= form.label :description ,"Descripción", class: "control-label col-sm-1" %>
              <div class="col-sm-11">
                <%= form.text_field :description, id: :audit_description, class: "form-control border" %>
              </div>
          </div>
         
     <div class="form-group">
            <%= form.label :active ,"Activo", class: "control-label col-sm-1" %>
              <div class="col-sm-11">
                <%= form.check_box :active, id: :audit_description, class: "form-control border" %>
              </div>
          </div>
          <div class="form-group">
            <%= form.label :cost ,"Costo", class: "control-label col-sm-1" %>
              <div class="col-sm-11">
                <%= form.text_field :cost, id: :audit_description, class: "form-control border" %>
              </div>
          </div>
           
        </div>

      </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
